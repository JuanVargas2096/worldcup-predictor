# ---- HTTP ----
quarkus.http.port=8080
quarkus.http.host=0.0.0.0

# ---- CORS (frontend Angular) ----
quarkus.http.cors=true
quarkus.http.cors.origins=/.*/
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.exposed-headers=Content-Disposition

# ---- Datasource (PostgreSQL) ----
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USERNAME:postgres}
quarkus.datasource.password=${DB_PASSWORD:postgres}
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/worldcup}
quarkus.datasource.jdbc.max-size=16

# ---- Hibernate ORM ----
# El esquema lo gestiona Flyway; Hibernate no genera nada (Quarkus 3.17).
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

# ---- Flyway ----
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.locations=db/migration
# Permite aplicar V8 (semilla generada por import_matches.sh) despues de V9/V10.
quarkus.flyway.out-of-order=true

# ---- App config ----
# Proveedor de datos de fútbol para la SIEMBRA al arranque: mock | external.
# 'mock' => arranque instantáneo sin llamadas HTTP (recomendado). Los datos reales
# del Mundial llegan por el scheduler (world_cup_*) y el botón "Importar" usa el
# provider configurado. Cambia a 'external' solo si quieres importar forma real al boot.
worldcup.data-provider=${DATA_PROVIDER:mock}
# Recalcular ranking automáticamente al iniciar
worldcup.recalculate-on-start=true

# REST Client - API Football
football-api/mp-rest/url=https://v3.football.api-sports.io
quarkus.rest-client.logging.scope=request-response
quarkus.rest-client.logging.body-limit=1024
quarkus.log.category."org.jboss.resteasy.reactive.client.logging".level=DEBUG
# La API key se inyecta por variable de entorno FOOTBALL_API_KEY (ver .env / docker-compose).
# Nunca se commitea la key real. Sin key (NO_KEY) la app funciona con datos simulados.
football.api.key=${FOOTBALL_API_KEY:NO_KEY}

# ---- Logging ----
quarkus.log.level=INFO
quarkus.log.category."com.worldcup".level=DEBUG
quarkus.log.file.rotation.file-suffix=.yyyy-MM-dd
quarkus.log.file.path=${LOG_PATH:./worldcup-predictor.log}
quarkus.log.file.enable=true
quarkus.log.console.format=%d{yyyy-MM-dd HH:mm:ss,SSS} [%c{1}.jar] [%t] %-5p (%M [%C:%L]) - %s%e%n
quarkus.log.file.format=%d{yyyy-MM-dd HH:mm:ss,SSS} [%c{1}.jar] [%t] %-5p (%M [%C:%L]) - %s%e%n
quarkus.console.color=true
